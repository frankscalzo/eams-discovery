# EAMS Environment Configuration
# Based on AWS Transfer project structure

# Environment
ENVIRONMENT=dev
STAGE=dev
PROJECT_NAME=eams
SERVICE_NAME=discovery

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=904233104383

# Domain Configuration
DOMAIN_NAME=optimumcloudservices.com
CUSTOM_DOMAIN=${STAGE}-${SERVICE_NAME}.${DOMAIN_NAME}
# dev-discovery.optimumcloudservices.com

# CloudFront Configuration
CLOUDFRONT_ALIASES=${CUSTOM_DOMAIN}

# SSL Certificate (must be in us-east-1 for CloudFront)
CERTIFICATE_ARN=arn:aws:acm:us-east-1:${AWS_ACCOUNT_ID}:certificate/your-certificate-id

# Cognito Configuration
COGNITO_DOMAIN_PREFIX=${STAGE}-${PROJECT_NAME}
USER_POOL_NAME=${STAGE}-${PROJECT_NAME}-users

# DynamoDB Configuration
DYNAMODB_TABLE_PREFIX=${STAGE}-${PROJECT_NAME}

# S3 Configuration
S3_BUCKET_PREFIX=${STAGE}-${PROJECT_NAME}

# API Gateway Configuration
API_GATEWAY_NAME=${STAGE}-${PROJECT_NAME}-api

# Tags
TAG_PROJECT=EAMS
TAG_ENVIRONMENT=${ENVIRONMENT}
TAG_OWNER=OptimumCloudServices